# Algolia DocSearch
#
# Fill these three values to enable Algolia search in Starlight.
# Without them, the site keeps using the default local Pagefind search.
ALGOLIA_APP_ID=
ALGOLIA_SEARCH_API_KEY=
ALGOLIA_INDEX_NAME=
