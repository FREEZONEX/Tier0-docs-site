---
title: Documentación de Tier0
description: Empieza a construir sobre el Unified Namespace de Tier0 — pega un prompt en tu agente de código y sigue las buenas prácticas.
tableOfContents: false
prev: false
next: false
---

<section class="t0-banner t0-frame accent not-content">
	<span class="t0-corners" aria-hidden="true"></span>
	<div class="t0-banner-main">
		<p class="t0-tag">Empieza a construir con Tier0</p>
		<h2>Pega esto en tu agente de código</h2>
		<p>Un solo prompt instala el CLI + skill de Tier0, inicia tu sesión y da la primera vuelta por el Unified Namespace.</p>
		<pre class="t0-prompt" id="t0-agent-prompt">{`Ejecuta \`npx @tier0/cli@latest install\` para instalar el CLI y el skill del agente.
Lee https://docs.tier0.app/llms.txt para la documentación completa de Tier0.`}</pre>
		<button class="t0-copy" id="t0-copy-btn">Copiar prompt</button>
	</div>
	<div class="t0-banner-side">
		<p>¿Prefieres hacerlo a mano?</p>
		<p class="t0-endpoints"><a href="/es/get-started/installation/">Guía de instalación</a> · <a href="/es/get-started/demo-factory/">Fábrica demo</a></p>
		<p>Endpoints legibles por máquina:</p>
		<p class="t0-endpoints"><a href="/llms.txt">/llms.txt</a></p>
	</div>
</section>

<script is:inline>{`
document.getElementById('t0-copy-btn').addEventListener('click', function () {
	var b = this;
	var text = document.getElementById('t0-agent-prompt').innerText;
	var done = function () {
		b.textContent = 'Copiado';
		setTimeout(function () { b.textContent = 'Copiar prompt'; }, 1600);
	};
	var fallback = function () {
		var ta = document.createElement('textarea');
		ta.value = text;
		ta.style.position = 'fixed';
		ta.style.opacity = '0';
		document.body.appendChild(ta);
		ta.select();
		try { document.execCommand('copy'); } catch (e) {}
		document.body.removeChild(ta);
		done();
	};
	if (navigator.clipboard && navigator.clipboard.writeText) {
		navigator.clipboard.writeText(text).then(done, fallback);
	} else {
		fallback();
	}
});
`}</script>

<div class="t0-explore not-content">
	<h2>Buenas prácticas</h2>
	<p>Patrones probados en despliegues reales de Tier0.</p>
</div>

<section class="t0-grid-wrap t0-frame not-content">
	<span class="t0-corners" aria-hidden="true"></span>
	<div class="t0-grid">
		<a class="t0-cell" href="/es/best-practice/uns-modeling/">
			<span class="t0-tag">Modelado</span>
			<h3>Modelado de UNS</h3>
			<p>Cómo está diseñado el Demo UNS — jerarquía, cómo evitar la explosión de topics y estructura de payload escalable.</p>
			<span class="t0-more">Leer más</span>
		</a>
		<a class="t0-cell" href="/es/best-practice/protocol-connections/">
			<span class="t0-tag">Protocolos</span>
			<h3>Conexión OPC UA / Modbus</h3>
			<p>Flujos de Node-RED para protocolos industriales típicos, con referencias JSON reales e importables.</p>
			<span class="t0-more">Leer más</span>
		</a>
		<a class="t0-cell" href="/es/best-practice/analytics-apps/">
			<span class="t0-tag">Analítica</span>
			<h3>Apps de analítica</h3>
			<p>Del query en Notebook a una app de análisis interactiva — el caso Aramco Bowtie (datos anonimizados).</p>
			<span class="t0-more">Leer más</span>
		</a>
	</div>
</section>
