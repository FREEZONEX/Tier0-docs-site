---
title: UNS データを扱う
description: "MQTT と API で UNS を操作し、API reference を参照します。"
editions: [edge, cloud, enterprise]
sidebar:
  order: 3
---
import { Steps } from '@astrojs/starlight/components';
import { Tabs, TabItem } from '@astrojs/starlight/components';

Tier0 では、MQTT と API を通じて UNS を操作できます。

## MQTT
Tier0 では、MQTT clients が credentials を使って UNS broker に接続できます。
:::note
**MQTTX**、**MQTT Explorer**、**Node-RED** などの MQTT clients を使って broker に接続し、messages を publish/subscribe できます。
:::
### MQTT Credentials を生成する
Tier0 の **Edge** ページで新しい credential を作成し、記録します。

### MQTT Clients を接続する

:::tip
以下の clients は、credentials を使って UNS broker に接続する方法を示す例です。
:::

<Tabs syncKey="mqtt-client">
  <TabItem label="MQTTX">

1. Install and open <a href="https://mqttx.app/" target="_blank" rel="noopener noreferrer">MQTTX</a>.
2. Add a connection with the information of the Tier0 UNS broker, and the generated credentials.
    ```json
    {
      "Host": "mqtt.tier0.dev",
      "Port": 8883,
      "Client ID": "<generated client ID>",
      "Username": "<generated username>",
      "Password": "<generated password>"
    }
    ``` 

  </TabItem>
  <TabItem label="MQTT Explorer">

1. Install and open <a href="https://mqtt-explorer.com/" target="_blank" rel="noopener noreferrer">MQTT Explorer</a>.
2. Add a new connection with the broker information and the generated credentials.
    ```json
    {
      "Protocol": "mqtt://",
      "Host": "mqtt.tier0.dev",
      "Port": 8883,
      "Username": "<generated username>",
      "Password": "<generated password>"
    }
    ``` 
3. Change the client ID to the credential ID in **ADVANCED** before connecting.

  </TabItem>
  <TabItem label="Node-RED">
      - Use **Event Flow** - Data processing module in Tier0 based on Node-RED.
          1. Create a flow in **Event Flow**, and start it with an `mqtt in` node.
          2. Select the UNS broker as **Server** (same name as the flow) and set **Topic** to a UNS model in the node.
      - Use **Separate Node-RED Instance**
          1. <a href="https://nodered.org/docs/getting-started/local" target="_blank" rel="noopener noreferrer">Node-RED</a> instance をインストールして開きます。
          2. Add a server in the `mqtt in` node with the following information.
          ```json
          {
            {/* under Connection */}
            "Server": "mqtt://mqtt.tier0.dev",
            "Port": 8883,
            "Client ID": "<generated clientId>",
            {/* under Security */}
            "Username": "<generated username>",
            "Password": "<generated password>"
          }
          ```
  </TabItem>
</Tabs>

### 接続をテストする
<Steps>
1. On the **Edge** page, make sure the status of each client under the **Clients** tab is normal.
2. Copy a UNS model topic and publish a message to it through the client.
3. Go back to **UNS**, check the data under the target topic.
</Steps>

## API
Use **RestAPI** to operate on UNS data, flows and authentication.
:::tip[API の使い方]
- OpenAPI base path は `/openapi/v1` です。`TIER0_BASE_URL` を Tier0 host と base path の組み合わせ（例：`https://your-workspace.tier0.app/openapi/v1`）に設定し、`x-api-key` header に API key を渡します。
- 右上の account icon をクリックし、**Settings > API Keys** で API keys を生成します。
:::

- `/flow/create`

  Source Flow または Event Flow を作成します。`flowName` と `flowType` は必須です。

  <Tabs syncKey="flow-api-language">
    <TabItem label="JavaScript">

      ```js
      const BASE_URL = process.env.TIER0_BASE_URL;
      const API_KEY = process.env.TIER0_API_KEY;

      const response = await fetch(`${BASE_URL}/flow/create`, {
        method: 'POST',
        headers: {
          'content-type': 'application/json',
          'x-api-key': API_KEY,
        },
        body: JSON.stringify({
          flowName: 'Manufacturing source flow',
          flowType: 'SourceFlow',
          description: 'Collects UNS source data from the plant.',
          template: '{}',
        }),
      });

      const result = await response.json();
      console.log(result.data.id, result.data.brokerID);
      ```

    </TabItem>
    <TabItem label="Python">

      ```python
      import os
      import requests

      base_url = os.environ["TIER0_BASE_URL"]
      api_key = os.environ["TIER0_API_KEY"]

      response = requests.post(
          f"{base_url}/flow/create",
          headers={"x-api-key": api_key},
          json={
              "flowName": "Manufacturing source flow",
              "flowType": "SourceFlow",
              "description": "Collects UNS source data from the plant.",
              "template": "{}",
          },
          timeout=30,
      )
      response.raise_for_status()

      result = response.json()
      print(result["data"]["id"], result["data"]["brokerID"])
      ```

    </TabItem>
    <TabItem label="cURL">

      ```bash
      curl -X POST "$TIER0_BASE_URL/flow/create" \
        -H "content-type: application/json" \
        -H "x-api-key: $TIER0_API_KEY" \
        -d '{
          "flowName": "Manufacturing source flow",
          "flowType": "SourceFlow",
          "description": "Collects UNS source data from the plant.",
          "template": "{}"
        }'
      ```

    </TabItem>
  </Tabs>

- `/flow/list`

  flows を一覧表示し、必要に応じて `flowType` または `keyword` で絞り込みます。UPDATE または GET の前に flow `id` を探すために使います。

  <Tabs syncKey="flow-api-language">
    <TabItem label="JavaScript">

      ```js
      const BASE_URL = process.env.TIER0_BASE_URL;
      const API_KEY = process.env.TIER0_API_KEY;

      const response = await fetch(`${BASE_URL}/flow/list`, {
        method: 'POST',
        headers: {
          'content-type': 'application/json',
          'x-api-key': API_KEY,
        },
        body: JSON.stringify({
          flowType: 'SourceFlow',
          keyword: 'Manufacturing',
        }),
      });

      const result = await response.json();
      console.log(result.data.list);
      ```

    </TabItem>
    <TabItem label="Python">

      ```python
      import os
      import requests

      base_url = os.environ["TIER0_BASE_URL"]
      api_key = os.environ["TIER0_API_KEY"]

      response = requests.post(
          f"{base_url}/flow/list",
          headers={"x-api-key": api_key},
          json={
              "flowType": "SourceFlow",
              "keyword": "Manufacturing",
          },
          timeout=30,
      )
      response.raise_for_status()

      result = response.json()
      print(result["data"]["list"])
      ```

    </TabItem>
    <TabItem label="cURL">

      ```bash
      curl -X POST "$TIER0_BASE_URL/flow/list" \
        -H "content-type: application/json" \
        -H "x-api-key: $TIER0_API_KEY" \
        -d '{
          "flowType": "SourceFlow",
          "keyword": "Manufacturing"
        }'
      ```

    </TabItem>
  </Tabs>

- `/flow/update`

  `id` で flow を更新します。変更したいフィールドだけを送信します。

  <Tabs syncKey="flow-api-language">
    <TabItem label="JavaScript">

      ```js
      const BASE_URL = process.env.TIER0_BASE_URL;
      const API_KEY = process.env.TIER0_API_KEY;
      const flowId = 123;

      const response = await fetch(`${BASE_URL}/flow/update`, {
        method: 'POST',
        headers: {
          'content-type': 'application/json',
          'x-api-key': API_KEY,
        },
        body: JSON.stringify({
          id: flowId,
          flowName: 'Manufacturing source flow v2',
          description: 'Updated source flow description.',
          isFavorite: 1,
        }),
      });

      const result = await response.json();
      console.log(result.data.success);
      ```

    </TabItem>
    <TabItem label="Python">

      ```python
      import os
      import requests

      base_url = os.environ["TIER0_BASE_URL"]
      api_key = os.environ["TIER0_API_KEY"]
      flow_id = 123

      response = requests.post(
          f"{base_url}/flow/update",
          headers={"x-api-key": api_key},
          json={
              "id": flow_id,
              "flowName": "Manufacturing source flow v2",
              "description": "Updated source flow description.",
              "isFavorite": 1,
          },
          timeout=30,
      )
      response.raise_for_status()

      result = response.json()
      print(result["data"]["success"])
      ```

    </TabItem>
    <TabItem label="cURL">

      ```bash
      curl -X POST "$TIER0_BASE_URL/flow/update" \
        -H "content-type: application/json" \
        -H "x-api-key: $TIER0_API_KEY" \
        -d '{
          "id": 123,
          "flowName": "Manufacturing source flow v2",
          "description": "Updated source flow description.",
          "isFavorite": 1
        }'
      ```

    </TabItem>
  </Tabs>

- `/flow/get`

  `id` で 1 つの flow を取得します。

  <Tabs syncKey="flow-api-language">
    <TabItem label="JavaScript">

      ```js
      const BASE_URL = process.env.TIER0_BASE_URL;
      const API_KEY = process.env.TIER0_API_KEY;
      const flowId = 123;

      const response = await fetch(`${BASE_URL}/flow/get`, {
        method: 'POST',
        headers: {
          'content-type': 'application/json',
          'x-api-key': API_KEY,
        },
        body: JSON.stringify({ id: flowId }),
      });

      const result = await response.json();
      console.log(result.data);
      ```

    </TabItem>
    <TabItem label="Python">

      ```python
      import os
      import requests

      base_url = os.environ["TIER0_BASE_URL"]
      api_key = os.environ["TIER0_API_KEY"]
      flow_id = 123

      response = requests.post(
          f"{base_url}/flow/get",
          headers={"x-api-key": api_key},
          json={"id": flow_id},
          timeout=30,
      )
      response.raise_for_status()

      result = response.json()
      print(result["data"])
      ```

    </TabItem>
    <TabItem label="cURL">

      ```bash
      curl -X POST "$TIER0_BASE_URL/flow/get" \
        -H "content-type: application/json" \
        -H "x-api-key: $TIER0_API_KEY" \
        -d '{"id": 123}'
      ```

    </TabItem>
  </Tabs>

## API リファレンス

- [Tier0 OpenAPI Reference](../../reference/tier0-openapi/)

## 次へ

- [UNS 上にアプリを構築](../build-apps/) — UNS データで産業アプリケーションを構築します。
- [UNS データを分析](../analyze-data/) — Marimo Notebook と Python で UNS データを分析します。
